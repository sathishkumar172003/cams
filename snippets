body('dob').
        custom(async (value, {req}) =>{
            let current_year = new Date().getFullYear()
            let birth_year = new Date(value).getFullYear()
            let age = current_year - birth_year
            if(age < 14){
                throw new Error(age)
            }
            return true
        }) 